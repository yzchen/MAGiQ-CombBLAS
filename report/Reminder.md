## Reminder of query running

1. diagonalize (.D()) = diagonalizeV(Row)

2. diagonalize (.T().D()) = diagonalizeV(Column)

3. left pivot multiplier = multDimApplyPrune(Column)

4. right pivot multiplier = multDimApplyPrune(Row)